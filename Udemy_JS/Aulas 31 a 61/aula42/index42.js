/* Escreva uma função chamada sPaisagem que
recebe doi argumentos, largura e altura de
uma imagem (number).
Retorne true se a imagem estiver no modo paisagem */

const ePaisagem = (lar, alt) => lar > alt;
console.log(ePaisagem(1920, 1080));
